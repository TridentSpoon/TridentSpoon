# TridentSpoon

Still don't know what it is I'm doing but I'm still indeed doing it.

Mostly poking at Linux desktop tooling — the small, annoying gaps where you
end up hand-editing a config file that a program could have handled for you.

## Out in the open

**[set-the-table](https://github.com/TridentSpoon/set-the-table)** — an
auto-mount FSTAB assistant. A GTK4 app for adding drives to `/etc/fstab` so
they mount automatically at boot, without hand-editing the file.

Everything else is still in drafts.

## Currently

Learning by building. Trying to git gud.
